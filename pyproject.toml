[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "youth-info-platform-monorepo"
version = "0.1.0"
description = "Monorepo metadata for the youth information platform"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
